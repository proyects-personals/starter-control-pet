/**
 * LanguageEnum
 *
 * @description
 * Enum que define los idiomas soportados por la aplicación.
 * Se utiliza como clave tipada para la gestión de idioma,
 * el mapeo de traducciones y la persistencia del estado.
 *
 * @example
 * LanguageEnum.EN
 * LanguageEnum.ES
 *
 * @version 1.0.0
 */
export enum LanguageEnum {
  EN = 'en',
  ES = 'es',
}